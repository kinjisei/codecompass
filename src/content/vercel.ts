import type { GlossaryEntry } from '../types'

export const vercel: GlossaryEntry[] = [
  {
    id: 'vl-vercel',
    categoryId: 'vercel',
    term: 'Vercel',
    short: 'Хостинг-платформа для фронтенда и serverless-функций — публикует твой сайт и держит его доступным по постоянному адресу.',
    detail:
      'Vercel специализируется на современных веб-приложениях (React, Vite, Next.js) и serverless-функциях. Ты подключаешь свой GitHub-репозиторий, и Vercel сам собирает проект и публикует его — без ручной настройки серверов. Есть щедрый бесплатный тариф, которого хватает для личных проектов, что и делает его частью твоего стека «только бесплатное».',
    example: 'После деплоя recall-app получит адрес вида recall-app.vercel.app, доступный с любого устройства, а не только с localhost.',
    related: ['web-hosting', 'vl-git-deploy'],
  },
  {
    id: 'vl-git-deploy',
    categoryId: 'vercel',
    term: 'Деплой через git push (CI/CD)',
    short: 'Каждый push в репозиторий автоматически запускает сборку и публикацию новой версии — без ручных действий.',
    detail:
      'Vercel следит за подключённым репозиторием: как только ты делаешь git push, он сам скачивает свежий код, запускает сборку (npm run build) и, если она прошла, публикует результат. Этот автоматический конвейер «изменение кода → сборка → выкладка» называется CI/CD (continuous integration / continuous deployment). Тебе не нужно вручную копировать файлы на сервер — весь процесс запускается сам от одного push.',
    example: 'Когда Recall будет на Vercel: правишь код → git push → через минуту новая версия уже открывается на телефоне. То же обновление, что в PWA происходит «само».',
    related: ['vl-vercel', 'git-push-pull', 'web-build'],
  },
  {
    id: 'vl-build-settings',
    categoryId: 'vercel',
    term: 'Build-настройки и вывод (output)',
    short: 'Vercel нужно знать, какой командой собрать проект и в какой папке лежит готовый результат.',
    detail:
      'При сборке Vercel запускает команду сборки (для Vite-проекта это npm run build) и берёт готовые статические файлы из выходной папки (у Vite это dist). Обычно он определяет эти настройки автоматически по типу проекта, но их можно задать и вручную. Понимание этого снимает магию: «деплой» — это просто «выполнить ту же сборку, что и локально, но на сервере Vercel, и раздавать полученную папку».',
    example: 'npm run build в recall-app создаёт папку dist — ровно её содержимое Vercel и будет отдавать пользователям.',
    related: ['web-build', 'vl-git-deploy'],
  },
  {
    id: 'vl-env-vars',
    categoryId: 'vercel',
    term: 'Переменные окружения на Vercel',
    short: 'Секреты (ключи API) задают не в файле .env.local, а в настройках проекта на сайте Vercel — иначе на сервере их не будет.',
    detail:
      'Файл .env.local лежит только на твоём компьютере и намеренно не попадает в репозиторий (он в .gitignore), поэтому на сервере Vercel его нет. Значит, серверные секреты нужно продублировать в панели Vercel (Project → Settings → Environment Variables). Там же действует то же правило префиксов: переменные с VITE_ попадут в клиентский код, без префикса — останутся только на сервере (в serverless-функции).',
    example: 'Для продакшена Recall ключ GEMINI_API_KEY нужно добавить именно в Environment Variables на Vercel — на компьютере он в .env.local, но туда сервер не заглядывает.',
    related: ['web-env', 'ai-api-key', 'vl-serverless'],
  },
  {
    id: 'vl-serverless',
    categoryId: 'vercel',
    term: 'Serverless-функции на Vercel (папка api/)',
    short: 'Любой файл в папке api/ автоматически становится серверным эндпоинтом — местом, где безопасно прятать секретный ключ.',
    detail:
      'Vercel превращает каждый файл в папке api/ в отдельную serverless-функцию, доступную по URL вида /api/имя. Функция получает запрос (req) и отвечает (res) — идеальное место для прокси к платному API, потому что её код не скачивается в браузер, и ключ остаётся скрытым. Файлы, начинающиеся с подчёркивания (например _core.ts), эндпоинтами не становятся — их держат как общий код.',
    example: 'В recall-app api/gemini.ts — эндпоинт /api/gemini, а api/_core.ts — общая логика вызова Gemini, которую он использует, но которая сама не является эндпоинтом.',
    related: ['baas-serverless-fn', 'pat-server-secret', 'vl-env-vars'],
  },
  {
    id: 'vl-vercel-json',
    categoryId: 'vercel',
    term: 'vercel.json и SPA-rewrite',
    short: 'Файл настроек Vercel; для одностраничного приложения (SPA) он говорит «любой адрес отдавай на index.html».',
    detail:
      'В SPA все «страницы» рисует JavaScript по URL, но реального файла /flashcards на сервере нет — есть один index.html. Если пользователь откроет /flashcards напрямую или обновит страницу, сервер по умолчанию вернёт «404 — не найдено». Правило rewrite в vercel.json перенаправляет все такие запросы на index.html, чтобы дальше уже React Router показал нужный экран. При этом важно не перекрыть этим правилом пути /api/*, которые должны идти в serverless-функции.',
    example: 'В recall-app vercel.json уже готов: делает SPA-rewrite и намеренно не трогает /api/*, чтобы эндпоинт Gemini продолжал работать.',
    related: ['rt-router', 'vl-serverless'],
  },
  {
    id: 'vl-preview',
    categoryId: 'vercel',
    term: 'Превью-деплой (preview deployment)',
    short: 'Отдельная временная версия сайта для каждой ветки или Pull Request — чтобы проверить изменения до выката в прод.',
    detail:
      'Кроме основного (production) деплоя из главной ветки, Vercel собирает отдельный превью-деплой для каждой другой ветки — со своим уникальным URL. Это позволяет посмотреть и протестировать новую фичу «вживую», не затрагивая рабочую версию, которой пользуются люди. Особенно полезно, когда над проектом работают несколько человек: каждый видит результат чужих изменений по ссылке до слияния в main.',
    example: 'Когда девушка в будущем начнёт вносить правки в Recall в отдельной ветке, Vercel даст ей превью-ссылку — проверить, не сломалось ли, прежде чем сливать в main.',
    related: ['git-branch', 'qa-environment', 'vl-git-deploy'],
  },
]
