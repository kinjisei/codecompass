import type { GlossaryEntry } from '../types'

export const git: GlossaryEntry[] = [
  {
    id: 'git-repo',
    categoryId: 'git',
    term: 'Репозиторий (repository)',
    short: 'Папка проекта, за историей изменений которой следит Git.',
    detail:
      'Как только выполнена команда git init в папке, Git начинает отслеживать все изменения файлов в ней — это и есть репозиторий («репа»). Внутри появляется скрытая папка .git, где хранится вся история. Репозиторий может жить только на компьютере (локальный) или ещё быть скопирован на GitHub (удалённый / remote) — для бэкапа и совместной работы.',
    example: 'Recall — это репозиторий: команда git log показывает его историю, а два коммита (45455d4 и ebbb0e8) — это записи в этой истории.',
    related: ['git-commit', 'git-remote'],
  },
  {
    id: 'git-commit',
    categoryId: 'git',
    term: 'Коммит (commit)',
    short: 'Сохранённый снимок состояния проекта в конкретный момент, с описанием «что изменилось и зачем».',
    detail:
      'Коммит — это как контрольная точка в игре: к этому состоянию кода всегда можно вернуться. У каждого коммита есть уникальный id (хэш, например 45455d4) и сообщение, объясняющее суть изменений. Хорошая практика — делать коммиты после каждого законченного маленького куска работы, а не раз в неделю одним гигантским комком.',
    example: 'В Recall коммит 45455d4 зафиксирован как «точка отката» — если что-то в будущем сломается, всегда можно вернуться именно к этому состоянию.',
    related: ['git-repo', 'git-diff', 'git-branch'],
  },
  {
    id: 'git-status',
    categoryId: 'git',
    term: 'git status',
    short: 'Команда: показывает, какие файлы изменены, добавлены или ещё не отслеживаются Git.',
    detail:
      'Это первая команда, которую стоит запускать, когда непонятно, в каком состоянии проект. Она честно перечисляет: какие файлы поменялись со времени последнего коммита, какие новые файлы Git ещё не видел (untracked), и что уже готово к коммиту (staged).',
    example: 'Перед тем как что-то закоммитить, Claude всегда сверяется с git status, чтобы не упустить забытые файлы.',
    related: ['git-commit', 'git-diff'],
  },
  {
    id: 'git-diff',
    categoryId: 'git',
    term: 'git diff',
    short: 'Команда: показывает построчные изменения — что именно добавлено или удалено с прошлого коммита.',
    detail:
      'Пока изменения не закоммичены, git diff — способ увидеть их построчно, зелёным / красным. Это то же самое, что «дифф» в общем смысле, но конкретно как команда терминала.',
    example: 'Перед коммитом Claude обычно смотрит git diff, чтобы описание коммита точно соответствовало реальным изменениям.',
    related: ['git-status', 'vc-diff'],
  },
  {
    id: 'git-add',
    categoryId: 'git',
    term: 'git add (staging area)',
    short: 'Команда, которая помечает файл как «готов войти в следующий коммит».',
    detail:
      'Git работает в два шага: сначала выбирается, какие изменения войдут в коммит (git add имя-файла — это и называется «staging», подготовка), а потом всё фиксируется одной командой git commit. Это позволяет закоммитить не всё подряд, а только законченную часть работы, даже если в проекте параллельно есть другие незавершённые правки.',
    example: 'git add src/App.tsx добавит в подготовку только этот файл, даже если рядом есть ещё пять изменённых файлов.',
    related: ['git-commit'],
  },
  {
    id: 'git-branch',
    categoryId: 'git',
    term: 'Ветка (branch)',
    short: 'Параллельная линия разработки — можно экспериментировать, не трогая основной рабочий код.',
    detail:
      'Основная ветка обычно называется main — это «боевая», стабильная версия проекта. Для рискованной идеи заводят отдельную ветку — работают там, и если всё получилось, «сливают» (merge) изменения обратно в main. Если не получилось — ветку просто удаляют, а main остаётся чистым.',
    example: 'Сейчас в Recall вся работа идёт прямо в main (это нормально для проекта в одиночку на раннем этапе), но когда девушка начнёт вносить свои правки параллельно — ветки станут полезны, чтобы не мешать друг другу.',
    related: ['git-repo', 'git-merge-conflict'],
  },
  {
    id: 'git-push-pull',
    categoryId: 'git',
    term: 'git push / git pull',
    short: 'push — отправить свои коммиты на GitHub; pull — забрать оттуда новые/чужие коммиты себе.',
    detail:
      'Локальный репозиторий живёт на компьютере. Чтобы был бэкап и доступ с других устройств, его связывают с «удалённым» репозиторием на GitHub (remote). git push отправляет новые коммиты туда, git pull — скачивает то, чего ещё нет локально (например, если кто-то другой закоммитил изменения).',
    example: 'Когда девушка в будущем начнёт вносить свои правки в Recall, обоим нужно будет делать git pull перед началом работы и git push после — чтобы не потерять чужие изменения.',
    related: ['git-repo', 'git-remote'],
  },
  {
    id: 'git-remote',
    categoryId: 'git',
    term: 'GitHub / remote',
    short: 'Копия репозитория, которая хранится в облаке (обычно на GitHub) — бэкап и точка синхронизации.',
    detail:
      'GitHub — самый популярный сайт для хранения Git-репозиториев в облаке. «Remote» — техническое слово для «удалённой копии» репозитория, с которой синхронизируются через push/pull. Помимо бэкапа GitHub даёт удобства: историю в браузере, Issues (баг-трекер), Pull Request (предложение изменений на ревью) — особенно пригодится, когда над проектом работает больше одного человека.',
    example: 'Профиль kyerbolat2004, видимый в статусе git этого проекта, скорее всего связан с GitHub-аккаунтом — туда можно запушить проект для бэкапа.',
    related: ['git-push-pull'],
  },
  {
    id: 'git-gitignore',
    categoryId: 'git',
    term: '.gitignore',
    short: 'Файл-список того, что Git должен игнорировать и никогда не сохранять в историю.',
    detail:
      'Некоторые файлы не должны попадать в репозиторий: секретные ключи (.env.local), временные файлы сборки, папка node_modules (она огромная и генерируется заново командой npm install). .gitignore — обычный текстовый файл со списком таких путей/масок, по одному на строку.',
    example: 'В Recall именно .gitignore не дал файлу .env.local с ключами Supabase попасть в репозиторий — это отдельно проверялось в первом коммите.',
    related: ['git-repo'],
  },
  {
    id: 'git-merge-conflict',
    categoryId: 'git',
    term: 'Конфликт слияния (merge conflict)',
    short: 'Ситуация, когда Git не может сам решить, чья версия строки кода правильная — просит выбрать вручную.',
    detail:
      'Возникает, когда два коммита (например, твой и девушкин, или два разных AI-сеанса) поменяли одну и ту же строку по-разному. Git честно помечает это прямо в файле специальными символами (<<<<<<<, =======, >>>>>>>) и просит человека вручную решить, какую версию оставить. Это не поломка — это подстраховка, чтобы никто не потерял работу молча.',
    example: 'Если ты и AI одновременно правите один файл в разных чатах и потом пытаетесь всё соединить — велик шанс словить именно такой конфликт.',
    related: ['git-branch'],
  },
]
