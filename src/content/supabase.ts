import type { GlossaryEntry } from '../types'

export const supabase: GlossaryEntry[] = [
  {
    id: 'sb-postgres',
    categoryId: 'supabase',
    term: 'PostgreSQL (Postgres)',
    short: 'Мощная реляционная база данных с открытым кодом, на которой построен Supabase.',
    detail:
      'Реляционная база хранит данные в таблицах со строками и колонками, а связи между таблицами задаются явно (например карточка «принадлежит» колоде через deck_id). Postgres — одна из самых надёжных и функциональных таких баз, полностью бесплатная. Supabase не изобретает свою базу, а даёт готовый управляемый Postgres в облаке плюс удобные надстройки вокруг него (авторизация, автогенерируемый API, панель управления).',
    example: 'В recall-app таблицы cards, decks, review_states, activity_log — это всё таблицы Postgres внутри проекта Supabase.',
    related: ['sb-schema', 'web-database'],
  },
  {
    id: 'sb-schema',
    categoryId: 'supabase',
    term: 'Схема базы (schema) и schema.sql',
    short: 'Описание структуры базы: какие есть таблицы, какие у них колонки, типы и связи. Часто хранится в файле schema.sql.',
    detail:
      'Схема — это «чертёж» базы: она фиксирует, что таблица cards имеет колонки id, front, back, deck_id и какого они типа. Держать схему в файле schema.sql в репозитории удобно тем, что структура базы становится видимой в коде и её история отслеживается через git — это единый источник правды по структуре данных, наравне с документом-контрактом архитектуры.',
    example: 'В recall-app docs/schema.sql описывает все таблицы и уже выполнен в Supabase — он же служит справочником, какие поля вообще есть.',
    related: ['sb-postgres', 'sb-migration', 'rt-typescript'],
  },
  {
    id: 'sb-migration',
    categoryId: 'supabase',
    term: 'Миграция (migration)',
    short: 'Записанное изменение структуры базы (добавить таблицу, колонку, индекс), которое можно применить и повторить.',
    detail:
      'База не переписывается целиком при каждом изменении — её меняют по шагам, и каждый шаг называется миграцией. Миграция — это обычно SQL-команда вроде «добавь такую-то колонку» или «создай такой-то индекс». Хранить миграции по порядку важно, чтобы структуру базы можно было воссоздать с нуля или повторить на другом окружении. В простом проекте миграцию иногда выполняют вручную через SQL Editor в панели Supabase.',
    example: 'В recall-app уникальный индекс для стрика добавляли именно так — SQL-командой через Supabase SQL Editor (та самая «вставь код и нажми Run», из-за которой в начале была путаница со «сниппетами»).',
    related: ['sb-sql-editor', 'sb-index', 'vc-snippet'],
  },
  {
    id: 'sb-sql-editor',
    categoryId: 'supabase',
    term: 'SQL Editor в панели Supabase',
    short: 'Окно на сайте Supabase, куда вставляют SQL-команды и выполняют их кнопкой Run — прямой доступ к базе.',
    detail:
      'Это веб-инструмент внутри дашборда Supabase (Project → SQL Editor → New query), а не часть твоего кода в VS Code. Сюда вставляют SQL, чтобы создать таблицу, добавить индекс, настроить политику доступа или просто посмотреть данные запросом. Важно не путать: это отдельное место на сайте Supabase, никак не связанное со сниппетами или папками в редакторе кода.',
    example: 'Именно сюда вставлялся create unique index ... для recall-app — «должно написать Success» относилось к результату выполнения в этом окне.',
    related: ['sb-migration', 'vc-snippet'],
  },
  {
    id: 'sb-sql',
    categoryId: 'supabase',
    term: 'SQL и базовые запросы',
    short: 'Язык запросов к реляционной базе: SELECT (прочитать), INSERT (добавить), UPDATE (изменить), DELETE (удалить).',
    detail:
      'SQL (Structured Query Language) — способ «разговаривать» с базой. Четыре главные операции: SELECT достаёт строки (можно с условием WHERE, сортировкой ORDER BY, лимитом LIMIT), INSERT добавляет новую строку, UPDATE меняет существующие, DELETE удаляет. Клиент Supabase оборачивает эти операции в удобные методы (.select(), .insert()), но под капотом это всё тот же SQL к Postgres.',
    example: 'В recall-app supabase.from(\'review_states\').select(\'*\').lte(\'due\', сейчас) — это, по сути, SELECT с условием WHERE due <= сейчас, только записанный методами клиента.',
    related: ['sb-postgres', 'web-database'],
  },
  {
    id: 'sb-index',
    categoryId: 'supabase',
    term: 'Индекс (index)',
    short: 'Служебная структура, ускоряющая поиск по колонке — как алфавитный указатель в конце книги.',
    detail:
      'Без индекса база при поиске просматривает все строки подряд (медленно на больших таблицах). Индекс по колонке заранее упорядочивает её значения, и база находит нужное почти мгновенно. Уникальный индекс дополнительно гарантирует, что значение не повторяется — например «не больше одной записи активности на день и тип». За скорость чтения платят чуть более медленной записью и местом на диске, поэтому индексы ставят на те колонки, по которым реально часто ищут.',
    example: 'В recall-app уникальный индекс на (user_id, day, type) в activity_log не даёт создать две записи активности за один день и одновременно ускоряет подсчёт стрика.',
    related: ['sb-migration', 'rt-typescript'],
  },
  {
    id: 'sb-rls-policy',
    categoryId: 'supabase',
    term: 'Политика RLS на практике',
    short: 'Конкретное правило вида «пользователь видит только строки, где owner_id равен его id» — пишется как условие прямо в базе.',
    detail:
      'RLS (Row Level Security) включается для таблицы, а затем к ней добавляют политики — правила для операций SELECT/INSERT/UPDATE/DELETE. Типичная политика использует auth.uid() (id текущего вошедшего пользователя) и разрешает доступ только к «своим» строкам: USING (owner_id = auth.uid()). Поскольку правило живёт в самой базе, оно защищает данные, даже если кто-то обратится к базе в обход твоего приложения — фронтенд физически не сможет получить чужие строки.',
    example: 'В recall-app именно поэтому «новизна» карточки считается персонально: RLS-политика на review_states отдаёт пользователю только его собственные записи расписания.',
    related: ['web-rls', 'sb-auth-deep'],
  },
  {
    id: 'sb-auth-deep',
    categoryId: 'supabase',
    term: 'Supabase Auth: провайдеры и сессия',
    short: 'Встроенный вход по email/паролю или через соцсети (Google и др.), с автоматическим управлением сессией и токенами.',
    detail:
      'Кроме email+пароль, Supabase Auth умеет вход через внешних провайдеров (Google, GitHub, Apple) по протоколу OAuth — их включают в настройках проекта. После входа выдаётся сессия с токеном, который клиент прикладывает к запросам, а база по нему понимает, кто это (auth.uid()) — на этом и строятся RLS-политики. Событие onAuthStateChange позволяет приложению реагировать на вход/выход, не опрашивая состояние постоянно.',
    example: 'В recall-app AuthContext подписывается на onAuthStateChange, а user.id из сессии — тот самый ключ, по которому RLS фильтрует данные пользователя.',
    related: ['web-auth', 'sb-rls-policy', 'rt-context'],
  },
  {
    id: 'sb-storage',
    categoryId: 'supabase',
    term: 'Supabase Storage (файлы)',
    short: 'Отдельное хранилище для файлов — картинок, аудио, документов — которые не помещают в таблицы базы.',
    detail:
      'Базу данных не используют для хранения самих файлов (это дорого и медленно) — в неё кладут только ссылку, а сам файл лежит в объектном хранилище. Supabase Storage — как раз такое хранилище, организованное в «бакеты» (папки-контейнеры), с теми же правилами доступа, что и таблицы (можно ограничить, кто какие файлы видит). Загруженный файл получает URL, который сохраняют в таблице.',
    example: 'В recall-app пока файлы не нужны, но если добавить, скажем, картинки к карточкам — их место именно в Storage, а в таблице cards хранилась бы только ссылка (audio_url устроен по этому принципу).',
    related: ['sb-postgres', 'kt-apk-aab'],
  },
  {
    id: 'sb-realtime',
    categoryId: 'supabase',
    term: 'Supabase Realtime',
    short: 'Возможность подписаться на изменения таблицы и получать их мгновенно, без ручного обновления.',
    detail:
      'По умолчанию приложение читает данные один раз по запросу. Realtime позволяет вместо этого «слушать» таблицу: когда где-то меняется строка, подписанные клиенты сразу получают обновление (push-модель). Это тот же принцип, что realtime-подписки в Firebase, только поверх Postgres. Полезно для совместной работы в реальном времени — когда изменения одного человека должны сразу видеть другие.',
    example: 'Пригодится в будущей Фазе 4 Recall («Преподаватель»): когда ученица выполняет задание, прогресс мог бы сразу появляться у преподавателя через Realtime, без обновления страницы.',
    related: ['baas-realtime'],
  },
]
