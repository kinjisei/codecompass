import type { GlossaryEntry } from '../types'

export const backendBaas: GlossaryEntry[] = [
  {
    id: 'baas-serverless-fn',
    categoryId: 'backend-baas',
    term: 'Serverless-функция',
    short: 'Код бэкенда, который выполняется по запросу или событию, а инфраструктурой (запуск, масштабирование) управляет провайдер, а не разработчик.',
    detail:
      'Вместо того чтобы держать постоянно запущенный сервер, пишут отдельные функции-обработчики, а платформа сама решает, когда их запускать и как масштабировать под нагрузку — отсюда «serverless» (сервер как бы есть, но им управляет не разработчик). Платят обычно только за реальные вызовы. Такие функции запускаются по разным триггерам: HTTP-запрос, создание пользователя, загрузка файла, расписание.',
    example: 'api/gemini.ts в recall-app (Vercel) и Cloud Functions вроде onAudioUploaded в gitlab-mobile-app (Firebase) — та же идея на двух разных платформах.',
    related: ['baas-firebase'],
  },
  {
    id: 'baas-firebase',
    categoryId: 'backend-baas',
    term: 'Firebase как Backend-as-a-Service',
    short: 'Облачная платформа Google — альтернатива Supabase: NoSQL-база документов, аутентификация, serverless-функции и хранилище файлов «из коробки».',
    detail:
      'В отличие от Supabase (готовый SQL/Postgres), Firebase хранит данные как документы в коллекциях (Cloud Firestore) — NoSQL-модель без жёсткой табличной схемы. Firebase Auth даёт готовый вход через email или соцсети (Google, Apple) по протоколу OAuth. Оба сервиса решают одну и ту же задачу — «бэкенд без написания своего сервера», — но по-разному моделируют данные: таблицы и SQL-запросы у Supabase против документов и коллекций у Firebase.',
    example: 'firebase_auth, cloud_functions и Firestore используются как весь бэкенд в gitlab-mobile-app и sybols_gitlab — вместо Supabase из recall-app.',
    related: ['baas-serverless-fn', 'web-supabase'],
  },
  {
    id: 'baas-webhook',
    categoryId: 'backend-baas',
    term: 'Webhook и event-driven pipeline',
    short: 'Внешний сервис сам делает HTTP-запрос на твой сервер, когда результат готов — вместо того чтобы твой код постоянно спрашивал «уже готово?».',
    detail:
      'Вместо того чтобы держать соединение открытым и ждать долгую задачу (например обработку аудио), систему строят на цепочке событий: одно действие порождает следующее. Webhook — это когда внешний сервис сам «перезванивает» на публичный URL твоего сервера, как только результат готов — это нужно, потому что мобильное приложение обычно недоступно снаружи напрямую (сидит за NAT оператора). Периодический опрос (polling) на всякий случай оставляют как запасной план — вдруг webhook потеряется.',
    example: 'sonioxWebhook и pollStuckTranscriptions как запасной план в архитектуре транскрипции проекта gitlab-mobile-app.',
    related: ['baas-serverless-fn', 'baas-realtime'],
  },
  {
    id: 'baas-realtime',
    categoryId: 'backend-baas',
    term: 'Realtime-подписка на изменения',
    short: 'Клиент подписывается на запись в базе и мгновенно получает уведомление при её изменении — без ручного опроса «а не обновилось ли».',
    detail:
      'Вместо того чтобы приложение периодически перезапрашивало «готово ли», оно один раз подписывается на конкретный документ/строку, и как только сервер меняет в ней данные, подписка сама «толкает» обновление клиенту — модель называется push, в противоположность pull (когда клиент сам инициирует каждый запрос). Источник истины остаётся на сервере, а интерфейс просто отражает пришедший поток изменений.',
    example: 'Подписка на снапшоты Firestore-документа transcriptions/{jobId} в gitlab-mobile-app; у Supabase, который используется в recall-app, для этого есть свой аналог — Supabase Realtime.',
    related: ['baas-webhook'],
  },
  {
    id: 'baas-secretmanager',
    categoryId: 'backend-baas',
    term: 'Secret Manager',
    short: 'Отдельное защищённое хранилище для секретных ключей API, из которого их получают только доверенные серверные функции.',
    detail:
      'Секреты платных внешних API нельзя ни хардкодить в коде, ни просто класть в переменные окружения бесконтрольно — их держат в специализированном сервисе (Google Secret Manager, аналоги есть у других облаков), который выдаёт значение только конкретным, явно авторизованным функциям, и хранит историю версий ключа.',
    example: 'SONIOX_API_KEY хранится в Google Secret Manager и выдаётся только конкретной Cloud Function в проекте gitlab-mobile-app.',
    related: ['pat-server-secret'],
  },
]
