import type { GlossaryEntry } from '../types'

export const jsBasics: GlossaryEntry[] = [
  {
    id: 'js-variable',
    categoryId: 'js-basics',
    term: 'Переменная (const и let)',
    short: 'Именованная «коробка» для значения. const — значение не переприсваивается, let — может меняться.',
    detail:
      'Переменная связывает имя со значением, чтобы обращаться к нему по имени, а не повторять само значение. const («константа») означает, что имя нельзя переприсвоить другому значению — это выбор по умолчанию, так безопаснее. let используют, когда значение действительно должно меняться со временем (счётчик, флаг). Старое ключевое слово var сегодня почти не используют из-за его коварного поведения с областью видимости.',
    example: 'const front = "hello" — слово карточки, которое не меняется; let count = 0 — счётчик повторённых карточек, который растёт.',
    related: ['js-type'],
  },
  {
    id: 'js-function',
    categoryId: 'js-basics',
    term: 'Функция',
    short: 'Именованный блок кода, который принимает входные данные (аргументы), что-то делает и обычно возвращает результат.',
    detail:
      'Функция — способ дать имя куску логики и переиспользовать его, не копируя. Она принимает аргументы (вход), выполняет действия и через return отдаёт результат (выход). Разбивка программы на маленькие функции с понятными именами — основа читаемого кода: имя функции объясняет, что она делает, без чтения её внутренностей. Функция, которая при тех же аргументах всегда возвращает то же и ничего вокруг не меняет, называется «чистой» (pure) — такие легче всего тестировать.',
    example: 'В recall-app функция addCard({ front, back }) принимает данные слова и возвращает созданную карточку — её зовут из разных мест, не дублируя логику.',
    related: ['js-arrow', 'js-async'],
  },
  {
    id: 'js-arrow',
    categoryId: 'js-basics',
    term: 'Стрелочная функция (=>)',
    short: 'Короткий синтаксис записи функции: (x) => x + 1 вместо длинной формы через function.',
    detail:
      'Стрелочная функция — компактный способ записать функцию, особенно удобный для «одноразовых» функций, передаваемых как аргумент (например обработчик клика или функция внутри map). Запись (a, b) => a + b означает «функция, берущая a и b и возвращающая их сумму». Если тело в одну строку — return можно опустить, значение вернётся само. Это самый частый вид функций в современном React-коде.',
    example: 'В React-коде recall-app: onClick={() => setRevealed(true)} — крошечная стрелочная функция, срабатывающая по клику.',
    related: ['js-function'],
  },
  {
    id: 'js-array',
    categoryId: 'js-basics',
    term: 'Массив',
    short: 'Упорядоченный список значений под одним именем, к которым обращаются по номеру (индексу), начиная с 0.',
    detail:
      'Массив хранит последовательность элементов: [10, 20, 30]. Первый элемент имеет индекс 0 (а не 1 — источник багов «на единицу»). Массивы перебирают методами: map (преобразовать каждый элемент в новый), filter (оставить подходящие), find (найти первый подходящий). Эти методы не меняют исходный массив, а возвращают новый — важное для предсказуемости свойство.',
    example: 'В CodeCompass allEntries — массив всех терминов; filter отбирает термины нужной категории, map превращает каждый в карточку на экране.',
    related: ['js-object'],
  },
  {
    id: 'js-object',
    categoryId: 'js-basics',
    term: 'Объект',
    short: 'Набор именованных полей (ключ: значение) под одним именем — способ хранить связанные данные вместе.',
    detail:
      'Объект группирует характеристики одной сущности: { front: "hello", back: "привет", ipa: "h…" }. К полю обращаются по имени через точку (card.front). В отличие от массива (доступ по номеру) у объекта доступ по осмысленному ключу. JSON — это по сути текстовая запись такого объекта для передачи между программами. Объекты — основной способ моделировать «вещи» в коде: карточку, пользователя, настройку.',
    example: 'Каждый термин в CodeCompass — объект GlossaryEntry с полями term, short, detail, example — ровно эта структура описана в src/types/index.ts.',
    related: ['js-array', 'js-type', 'web-json'],
  },
  {
    id: 'js-type',
    categoryId: 'js-basics',
    term: 'Тип данных',
    short: 'Категория значения: строка (текст), число, булево (да/нет), массив, объект, null/undefined (пусто).',
    detail:
      'Каждое значение принадлежит типу: string (текст в кавычках), number (число), boolean (true/false), объект, массив. Отдельно есть «пустые» значения: null (осознанное «ничего») и undefined (значение вообще не задано) — попытка обратиться к их полям и есть та самая частая ошибка «cannot read properties of null». В обычном JavaScript тип переменной может незаметно меняться, что порождает баги; TypeScript добавляет проверку типов заранее, чтобы этого не случалось.',
    example: 'Ошибка «cannot read properties of null» в стектрейсе (см. QA-тему) почти всегда значит: код ждал объект, а получил null — данные ещё не загрузились или их нет.',
    related: ['js-object', 'rt-typescript', 'qa-stacktrace'],
  },
  {
    id: 'js-conditional',
    categoryId: 'js-basics',
    term: 'Условие (if / else) и булева логика',
    short: 'Развилка: «если условие истинно — сделай одно, иначе — другое». Основа любой логики принятия решений в коде.',
    detail:
      'if проверяет условие (выражение, дающее true или false) и выполняет блок, только если оно истинно; else — что делать иначе. Условия строят из сравнений (=== равно, !== не равно, > больше) и логических связок (&& «и», || «или», ! «не»). Важная тонкость JavaScript: === сравнивает строго, а «пустые» значения (0, "", null) ведут себя как false в условии — источник неожиданных багов на edge case-ах.',
    example: 'В карточке CodeCompass: if (revealed) показать ответ, else показать кнопку «Показать ответ» — простая развилка по состоянию.',
    related: ['js-type', 'qa-edge-case'],
  },
  {
    id: 'js-async',
    categoryId: 'js-basics',
    term: 'Асинхронность (async / await, Promise)',
    short: 'Способ дождаться долгой операции (запрос к серверу, чтение файла), не «замораживая» всё остальное.',
    detail:
      'Некоторые операции занимают время: запрос в базу, вызов API, загрузка файла. Promise — «обещание» результата, который придёт позже. Функция, помеченная async, может внутри писать await перед таким обещанием — код читается сверху вниз как обычный, но не блокирует интерфейс на время ожидания. Это та же идея, что Future/async в Dart из твоих Flutter-проектов — общий для многих языков механизм.',
    example: 'В recall-app await addCard(...) ждёт, пока карточка запишется в Supabase, и только потом продолжает — но интерфейс в это время не виснет.',
    related: ['js-function', 'rt-promise-all', 'flt-async'],
  },
]
