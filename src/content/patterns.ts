import type { GlossaryEntry } from '../types'

export const patterns: GlossaryEntry[] = [
  {
    id: 'pat-clean-arch',
    categoryId: 'patterns',
    term: 'Слоистая архитектура (data / domain / presentation)',
    short: 'Разделение кода на три слоя, где зависимости направлены только внутрь — к «домену», не знающему ни о UI, ни о базе данных.',
    detail:
      'domain — чистая бизнес-логика (сущности, контракты репозиториев, use case), не зависящая ни от фреймворка, ни от UI, ни от конкретной БД. data — реализации: работа с API/БД, DTO, мапперы; реализует контракты, описанные в domain. presentation — экраны и управление их состоянием. Правило: presentation и data знают только про domain и не знают друг про друга напрямую. Это упрощает тесты (domain тестируется без UI и без сети) и позволяет заменить источник данных, не трогая бизнес-логику.',
    example: 'Такое разделение — основа архитектуры в gitlab-mobile-app и sybols_gitlab: папки data/, domain/, presentation/ в каждом модуле.',
    related: ['pat-repository', 'flt-monorepo'],
  },
  {
    id: 'pat-repository',
    categoryId: 'patterns',
    term: 'Repository pattern и Use Case',
    short: 'Repository прячет источник данных за интерфейсом; Use Case — один конкретный сценарий бизнес-логики.',
    detail:
      'Repository — это контракт (абстрактный класс/интерфейс) в domain-слое, описывающий «что можно сделать с данными» (например getSettings()), а конкретная реализация в data-слое решает «откуда» — из локальной БД, сети или файла. UI и логика зависят только от интерфейса, поэтому источник можно заменить, не трогая остальной код. Use Case (интерактор) инкапсулирует один сценарий («сохранить язык») и вызывает репозитории — так бизнес-действия становятся явными, именованными и переиспользуемыми, а не размазанными по коду.',
    example: 'settings_repository.dart (контракт) и settings_repository_impl.dart (реализация) в gitlab-mobile-app; use case-ы собраны в отдельной папке domain/.../use_cases.',
    related: ['pat-clean-arch', 'pat-dto'],
  },
  {
    id: 'pat-dto',
    categoryId: 'patterns',
    term: 'DTO и Mapper',
    short: 'DTO — «сырой» объект, повторяющий формат внешних данных; Mapper переводит его в доменную модель приложения.',
    detail:
      'DTO (Data Transfer Object) — модель, повторяющая форму данных из внешнего источника (JSON от API, запись в БД), и её держат в слое данных, не пуская в бизнес-логику — иначе формат хранения «протечёт» в остальной код. Mapper — функция или класс, конвертирующая DTO в доменную сущность и обратно. Плюс: при смене формата API или БД правится только маппер, а не весь проект насквозь.',
    example: 'Правило «Map DTO ↔ domain entities via mappers, не пускать DTO дальше data-слоя» зафиксировано в стайл-гайде проекта gitlab-mobile-app.',
    related: ['pat-repository'],
  },
  {
    id: 'pat-solid',
    categoryId: 'patterns',
    term: 'SOLID, DRY, KISS, YAGNI',
    short: 'Набор инженерных принципов, чтобы код оставался простым, читаемым и расширяемым, а не превращался в хаос.',
    detail:
      'DRY (Don\'t Repeat Yourself) — не дублировать одну и ту же логику в нескольких местах. KISS (Keep It Simple) — выбирать простейшее решение, которое реально работает, вместо избыточно сложного. YAGNI (You Aren\'t Gonna Need It) — не добавлять функциональность «на всякий случай», пока она реально не понадобилась. SOLID — пять принципов ООП; из них особенно практичны Single Responsibility (один класс — одна ответственность) и Dependency Inversion (зависеть от абстракций, а не от конкретных реализаций — отсюда растут DI и репозитории-контракты).',
    example: 'Правило «1 файл = 1 класс» и отдельный раздел про эти принципы есть в документации проекта gitlab-mobile-app.',
    related: ['flt-di', 'pat-repository'],
  },
  {
    id: 'pat-adapter',
    categoryId: 'patterns',
    term: 'Обёртка над библиотекой (Adapter)',
    short: 'Свой тонкий слой поверх внешней библиотеки, скрывающий её детали и API за собственными типами.',
    detail:
      'Вместо того чтобы звать библиотеку напрямую из компонентов/экранов по всему приложению, её оборачивают в один файл со своими типами и функциями. Внутри — перевод между «своими» понятиями и понятиями библиотеки (например маппинг статусов). Это уменьшает связанность: если библиотеку когда-то заменят, менять придётся один файл-обёртку, а не весь проект.',
    example: 'В recall-app библиотека ts-fsrs спрятана за файлом lib/fsrs.ts со своими типами Rating и ReviewState — компоненты не знают о самой библиотеке.',
  },
  {
    id: 'pat-config-data',
    categoryId: 'patterns',
    term: 'Конфигурация как данные (data-driven design)',
    short: 'Правила поведения программы вынесены в списки объектов-настроек, а не «зашиты» условиями внутри логики.',
    detail:
      'Вместо разбросанных по коду if-ов параметры (уровни сложности, темы оформления, тарифы) описывают как список неизменяемых объектов-конфигов, а код просто читает эти данные и ведёт себя соответственно. Добавить новый уровень или тему — значит добавить запись в список, не трогая игровую/бизнес-логику. Вычисляемые свойства выводят производные значения прямо из полей конфига.',
    example: 'В spatial_memory_game списки gameThemes и difficulties с геттерами вроде pointsFor(...) задают все уровни игры без единого if в коде экрана.',
  },
  {
    id: 'pat-single-source',
    categoryId: 'patterns',
    term: 'Единый источник истины (и риск дублирования логики)',
    short: 'Одни и те же правила/данные должны существовать только в одном месте — иначе копии рано или поздно разойдутся и всё сломается незаметно.',
    detail:
      'Когда одна и та же логика (например, правило превращения слова в имя файла) продублирована в двух разных частях системы, они обязаны совпадать «символ в символ» — иначе одна часть будет искать то, чего не создала другая. Явные комментарии-предупреждения в коде («поменял тут — поменяй и там») — признак того, что дублирования на самом деле стоило избежать, вынеся правило в общее место.',
    example: 'В spatial_memory_game словари транслитерации имён дублируются в самом приложении и в отдельном скрипте-генераторе озвучки — с явным комментарием-предупреждением об этом риске.',
  },
  {
    id: 'pat-state-machine',
    categoryId: 'patterns',
    term: 'Конечный автомат (state machine)',
    short: 'Система, которая в каждый момент находится ровно в одном из чётко заданных состояний и переходит между ними только по разрешённым событиям.',
    detail:
      'Сложную логику с фазами (например: «озвучка → прослушивание → распознано → показ ошибки») удобно описывать как конечный автомат — набор состояний и правил перехода между ними. Каждое состояние знает, какие переходы из него допустимы; попытка недопустимого перехода просто игнорируется (через проверку-«guard»). Это устраняет невозможные комбинации состояния и делает поведение программы предсказуемым и легко тестируемым.',
    example: 'Статусы вроде speaking/listening/completed и переходы между ними по событиям — основа речевой игры в проекте sybols_gitlab.',
    related: ['flt-bloc'],
  },
  {
    id: 'pat-server-secret',
    categoryId: 'patterns',
    term: 'Server-mediated secrets (секрет живёт только на сервере)',
    short: 'Секретный ключ внешнего API никогда не попадает в мобильное приложение или браузер — за него ходит отдельный серверный код-посредник.',
    detail:
      'Если положить платный API-ключ прямо в мобильное приложение, его легко извлечь из установочного файла — и кто угодно потратит чужие деньги. Паттерн: клиент обращается к своему бэкенду (serverless-функции), а тот уже с ключом из защищённого хранилища (например Google Secret Manager) зовёт внешний сервис. Дополнительно можно защитить обратные вызовы (webhook) отдельным shared-секретом в заголовке запроса.',
    example: 'SONIOX_API_KEY хранится в Secret Manager и используется только внутри Cloud Function в проекте gitlab-mobile-app — мобильное приложение этого ключа не видит вовсе. Похожий приём есть и в recall-app: секретный GEMINI_API_KEY живёт только в serverless-функции на Vercel.',
    related: ['web-env', 'baas-secretmanager'],
  },
  {
    id: 'pat-deterministic',
    categoryId: 'patterns',
    term: 'Детерминированность и seed',
    short: 'Функция детерминирована, если при одних и тех же входных данных всегда возвращает один и тот же результат — это основа тестируемости.',
    detail:
      'Там, где программе нужна «случайность» (генерация упражнений, выбор неверного варианта ответа), используют генератор случайных чисел с seed — стартовым значением: при одном и том же seed последовательность «случайных» чисел повторяется один в один. Это позволяет воспроизвести конкретный сценарий теста или баг-репорта, а не гадать, что именно произошло у пользователя.',
    example: 'Требование «генерация сигналов детерминируема через seed — для воспроизводимости тестов» явно зафиксировано в документации модуля signals проекта sybols_gitlab.',
  },
]
