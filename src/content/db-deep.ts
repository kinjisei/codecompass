import type { GlossaryEntry } from '../types'

export const dbDeep: GlossaryEntry[] = [
  {
    id: 'dd-table-row-col',
    categoryId: 'db-deep',
    term: 'Таблица, строка, колонка',
    short: 'Таблица — как лист Excel: колонки задают поля (что храним), строки — отдельные записи (конкретные данные).',
    detail:
      'Реляционная база хранит данные в таблицах. Колонка (столбец) — это поле с именем и типом: например у таблицы cards колонки id, front, back. Строка (запись) — один конкретный объект: одна карточка со своими значениями во всех колонках. Одна таблица обычно хранит один вид сущностей: карточки — в cards, колоды — в decks. Понимание «таблица = вид данных, строка = один экземпляр» — фундамент всего остального.',
    example: 'В Recall таблица cards: колонки (id, front, back, example…) описывают, что храним про слово; каждая строка — одно конкретное слово.',
    related: ['web-database', 'sb-postgres'],
  },
  {
    id: 'dd-primary-key',
    categoryId: 'db-deep',
    term: 'Первичный ключ (primary key)',
    short: 'Колонка, уникально идентифицирующая каждую строку — как номер паспорта. Чаще всего это id.',
    detail:
      'Чтобы однозначно сослаться на конкретную строку, у таблицы есть первичный ключ — колонка (или набор), значение которой уникально и не пустое. Обычно это id — автогенерируемое число или случайный идентификатор (UUID). По первичному ключу база находит строку мгновенно и гарантирует, что двух одинаковых id не будет. Это «якорь» записи, на который ссылаются другие таблицы.',
    example: 'В таблице cards Recall колонка id — первичный ключ; именно на него ссылается таблица review_states через card_id, чтобы связать оценку с карточкой.',
    related: ['dd-foreign-key', 'sb-index'],
  },
  {
    id: 'dd-foreign-key',
    categoryId: 'db-deep',
    term: 'Внешний ключ (foreign key)',
    short: 'Колонка, которая ссылается на первичный ключ другой таблицы — так таблицы связываются между собой.',
    detail:
      'Внешний ключ — это «указатель» из одной таблицы на строку в другой. Вместо того чтобы дублировать данные, хранят ссылку: в таблице review_states колонка card_id содержит id карточки из таблицы cards. База может следить за целостностью: не даст сослаться на несуществующую карточку и подскажет, что делать при удалении. Внешние ключи — это то, что превращает разрозненные таблицы в связанную систему.',
    example: 'В Recall review_states.card_id (внешний ключ) → cards.id (первичный ключ), а review_states.user_id → пользователь; так оценка привязана и к карточке, и к тебе.',
    related: ['dd-primary-key', 'dd-relations'],
  },
  {
    id: 'dd-relations',
    categoryId: 'db-deep',
    term: 'Связи: один-ко-многим и многие-ко-многим',
    short: 'Как таблицы относятся друг к другу: у одной колоды много карточек (1-ко-многим); карточка в разных колодах (многие-ко-многим).',
    detail:
      'Один-ко-многим — самая частая связь: одна строка в таблице A связана со многими в таблице B (одна колода — много карточек), реализуется внешним ключом на стороне «многих» (у карточки есть deck_id). Многие-ко-многим — когда с обеих сторон «много» (карточка может быть в нескольких колодах, а колода содержит много карточек); её делают через отдельную промежуточную таблицу-связку с двумя внешними ключами. Умение видеть эти связи — ключ к пониманию любой схемы БД.',
    example: 'В Recall связь «одна колода → много карточек» (deck_id у карточки) — это один-ко-многим; типичная схема, которую ты видишь в docs/schema.sql.',
    related: ['dd-foreign-key', 'dd-normalization'],
  },
  {
    id: 'dd-join',
    categoryId: 'db-deep',
    term: 'JOIN (соединение таблиц)',
    short: 'Запрос, собирающий данные из нескольких связанных таблиц в один результат — например карточку вместе с её расписанием.',
    detail:
      'Раз данные разложены по разным таблицам, часто нужно собрать их обратно: показать карточку вместе с её оценкой/расписанием. JOIN соединяет строки двух таблиц по совпадению ключей (review_states.card_id = cards.id) и отдаёт объединённый результат. Виды: INNER (только совпавшие в обеих), LEFT (все из левой + совпавшие из правой, даже если пары нет). Клиент Supabase прячет JOIN за удобным синтаксисом вложенного выбора.',
    example: 'В fsrs.ts Recall запрос .select(\'*, cards(*)\') к review_states — это по сути JOIN: вернуть расписание вместе с данными связанной карточки.',
    related: ['dd-foreign-key', 'sb-sql', 'net-rest'],
  },
  {
    id: 'dd-normalization',
    categoryId: 'db-deep',
    term: 'Нормализация (не дублируй данные)',
    short: 'Принцип: каждый факт хранится в одном месте. Вместо копий — ссылки, чтобы данные не разъезжались.',
    detail:
      'Нормализация — раскладывание данных так, чтобы ничего не дублировалось. Если бы имя колоды хранилось в каждой её карточке, при переименовании пришлось бы менять сотни строк, и часть наверняка «отстанет». Правильно: имя колоды хранится один раз в таблице decks, а карточки лишь ссылаются на неё через deck_id. Один факт — одно место. Это ровно та же идея «единого источника истины», что и в архитектуре кода, только для данных.',
    example: 'В Recall перевод и пример хранятся в самой карточке один раз, а не копируются в каждую оценку — оценки лишь ссылаются на карточку по id.',
    related: ['dd-relations', 'pat-single-source'],
  },
  {
    id: 'dd-transaction',
    categoryId: 'db-deep',
    term: 'Транзакция (всё или ничего)',
    short: 'Группа изменений, которая применяется целиком либо не применяется вовсе — чтобы данные не остались «наполовину».',
    detail:
      'Иногда одно действие требует нескольких изменений сразу (списать деньги у одного, зачислить другому). Транзакция гарантирует «всё или ничего»: если посреди операции что-то сорвётся, база откатит все изменения, как будто ничего не было, — не останется состояния «списали, но не зачислили». Это защищает целостность данных при сбоях и одновременных операциях. Для одиночных простых записей транзакция не нужна, но знать о ней важно.',
    example: 'В простом Recall транзакции пока не нужны, но будущий режим «Преподаватель» (назначить колоду сразу нескольким ученицам) — кандидат: либо всем назначилось, либо никому.',
    related: ['dd-relations', 'pat-deterministic'],
  },
  {
    id: 'dd-null-default',
    categoryId: 'db-deep',
    term: 'NULL и значения по умолчанию',
    short: 'NULL — «значения нет вообще» (не ноль и не пустая строка). Default — что подставить, если значение не задали.',
    detail:
      'NULL означает отсутствие значения — это не 0 и не "", а именно «неизвестно/не задано», и сравнивается по особым правилам (это частый источник багов и на фронте, и в запросах). Колонку можно объявить NOT NULL, чтобы запретить пустоту, или задать ей DEFAULT — значение, которое подставится автоматически, если при вставке его не указали (например created_at = сейчас). Понимание NULL напрямую связано с ошибкой «cannot read properties of null» во фронтенде.',
    example: 'В Recall у карточки back (перевод) может быть NULL — слово без перевода; поэтому в коде перед показом проверяют его наличие, чтобы не упасть на null.',
    related: ['js-type', 'qa-stacktrace', 'dd-table-row-col'],
  },
]
