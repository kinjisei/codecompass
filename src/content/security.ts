import type { GlossaryEntry } from '../types'

export const security: GlossaryEntry[] = [
  {
    id: 'sec-authn-authz',
    categoryId: 'security',
    term: 'Аутентификация и авторизация',
    short: 'Аутентификация — «кто ты» (вход); авторизация — «что тебе можно» (права). Разные вещи, их часто путают.',
    detail:
      'Сначала система выясняет, кто ты (аутентификация — по паролю, коду, соцсети), потом решает, что тебе разрешено (авторизация — можешь ли ты открыть этот раздел, удалить эту запись). Дыры бывают в обоих: слабый вход (легко подобрать пароль) — проблема аутентификации; «обычный пользователь смог открыть админку» — проблема авторизации. Как QA полезно проверять именно второе: а что будет, если зайти туда, куда не положено.',
    example: 'В recall-app Supabase Auth отвечает за аутентификацию, а RLS-политики — за авторизацию (какие строки данных тебе видны). Это два разных механизма.',
    related: ['web-auth', 'sb-rls-policy'],
  },
  {
    id: 'sec-secrets',
    categoryId: 'security',
    term: 'Секреты и почему их прячут на сервере',
    short: 'Ключи API, пароли, токены нельзя класть в код фронтенда — браузер скачивает его целиком, и секрет утечёт.',
    detail:
      'Весь код фронтенда (JavaScript) загружается в браузер пользователя, и его можно прочитать через инструменты разработчика. Поэтому секретный ключ, вписанный в React-код, по сути публичен — кто угодно потратит твой лимит или влезет в данные. Правильно: секрет живёт только на сервере (в переменной окружения serverless-функции), а фронтенд обращается к своему серверу-посреднику. Публичные ключи (как anon-ключ Supabase) — исключение: они и задуманы открытыми, а защищает данные RLS.',
    example: 'В recall-app ключ Gemini лежит без префикса VITE_ в serverless-функции на Vercel; VITE_SUPABASE_ANON_KEY — наоборот, публичный по замыслу.',
    related: ['pat-server-secret', 'web-env', 'ai-api-key', 'vl-env-vars'],
  },
  {
    id: 'sec-xss',
    categoryId: 'security',
    term: 'XSS (межсайтовый скриптинг)',
    short: 'Атака, где чужой JavaScript выполняется на твоей странице — например через данные, которые пользователь ввёл, а сайт вставил как есть.',
    detail:
      'Если сайт берёт введённый пользователем текст и вставляет его в страницу без обработки, злоумышленник может ввести не текст, а <script>, который выполнится у других пользователей — украдёт куки, подменит страницу. Защита: экранировать (обезвредить) пользовательский ввод при выводе. React делает это автоматически — вставленный текст показывается как текст, а не как код; опасность возникает, только если намеренно использовать dangerouslySetInnerHTML.',
    example: 'CodeCompass и Recall безопасны по умолчанию: React выводит текст карточек как текст. Риск появился бы, если бы кто-то вставлял HTML пользователя напрямую в DOM.',
    related: ['net-cookie', 'html-dom'],
  },
  {
    id: 'sec-injection',
    categoryId: 'security',
    term: 'SQL-инъекция',
    short: 'Атака, где введённые данные подмешиваются в запрос к базе как команда, а не как значение.',
    detail:
      'Если запрос к базе собирают склейкой строк с пользовательским вводом, злоумышленник может ввести кусок SQL, который база выполнит — прочитает или удалит чужие данные. Защита — параметризованные запросы: данные передаются отдельно от текста запроса, и база никогда не воспринимает их как команду. Клиентские библиотеки (как у Supabase) делают это за тебя, если пользоваться их методами, а не склеивать SQL руками.',
    example: 'Методы Supabase вроде .eq(\'user_id\', id) в recall-app параметризованы — значение id не может «сломать» запрос, даже если в нём спецсимволы.',
    related: ['sb-sql', 'web-database'],
  },
  {
    id: 'sec-https',
    categoryId: 'security',
    term: 'HTTPS и TLS-сертификат',
    short: 'Шифрование канала между браузером и сервером плюс подтверждение, что сервер действительно тот, за кого себя выдаёт.',
    detail:
      'TLS (раньше назывался SSL) шифрует данные в пути — их нельзя подсмотреть или подменить в открытом Wi-Fi. Сертификат — цифровой «паспорт» сайта, выданный доверенным центром, подтверждающий подлинность домена; браузер показывает замок, если всё ок. Без HTTPS PWA вообще не устанавливается и часть браузерных возможностей отключена. На хостингах вроде Vercel сертификат выдаётся и обновляется автоматически.',
    example: 'CodeCompass получил HTTPS-сертификат от Vercel автоматически — иначе его нельзя было бы установить как приложение на Samsung S25.',
    related: ['net-http', 'web-hosting'],
  },
  {
    id: 'sec-validation',
    categoryId: 'security',
    term: 'Проверка ввода (валидация) и «не доверяй клиенту»',
    short: 'Данные от пользователя всегда проверяют на сервере — фронтенд-проверки удобны, но их легко обойти.',
    detail:
      'Проверки в интерфейсе (обязательное поле, формат email) улучшают удобство, но не безопасность: любой может отправить запрос в обход твоей страницы, минуя эти проверки. Поэтому важные правила дублируют на сервере/в базе, где их не обойти (в т.ч. через ограничения и RLS). Принцип «никогда не доверяй данным от клиента» — базовый в безопасности. Для тебя как QA это подсказка: пробуй отправлять «плохие» данные напрямую, а не только через форму.',
    example: 'В recall-app пустая карточка не сохраняется — но настоящая гарантия не в проверке формы, а в правилах на стороне Supabase (ограничения таблицы + RLS).',
    related: ['qa-boundary', 'sb-rls-policy', 'html-form'],
  },
  {
    id: 'sec-deps',
    categoryId: 'security',
    term: 'Уязвимости в зависимостях',
    short: 'Твоё приложение содержит сотни чужих библиотек — в любой может найтись дыра, которую надо вовремя обновить.',
    detail:
      'Современный проект тянет десятки прямых и сотни косвенных зависимостей (node_modules). Если в какой-то из них найдут уязвимость, она автоматически становится и твоей. Инструменты вроде npm audit сканируют зависимости на известные проблемы и подсказывают, что обновить. Отсюда важность lock-файла (фиксирует версии) и регулярных обновлений — но осторожных, чтобы обновление само не сломало сборку.',
    example: 'Команда npm audit в проекте CodeCompass показала «found 0 vulnerabilities» при установке — значит известных дыр в зависимостях нет.',
    related: ['npm-audit', 'npm-node-modules'],
  },
]
