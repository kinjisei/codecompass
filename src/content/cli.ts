import type { GlossaryEntry } from '../types'

export const cli: GlossaryEntry[] = [
  {
    id: 'cli-shell',
    categoryId: 'cli',
    term: 'Оболочка (shell): bash, PowerShell',
    short: 'Программа, которая принимает твои текстовые команды и выполняет их. bash — в Linux/Mac, PowerShell — в Windows.',
    detail:
      'Оболочка — это «переводчик» между тобой и операционной системой: ты пишешь команду, она её выполняет и показывает результат. Разные системы — разные оболочки и немного разный синтаксис: bash/zsh в Linux и macOS, PowerShell и cmd в Windows. У тебя Windows, поэтому основная оболочка — PowerShell, но многие инструменты (включая Claude) умеют и в bash-совместимый режим (Git Bash).',
    example: 'В твоём окружении команды идут через PowerShell, а часть скриптов Claude запускает в Git Bash — это две разные оболочки на одном компьютере.',
    related: ['vc-terminal', 'cli-command'],
  },
  {
    id: 'cli-command',
    categoryId: 'cli',
    term: 'Команда, аргументы и флаги',
    short: 'Команда — что сделать (ls), аргумент — над чем (ls src), флаг — как именно (ls -la). Пробелы разделяют части.',
    detail:
      'Строка команды состоит из имени программы и того, что ей передают. Аргументы — основные данные (какой файл, какая папка). Флаги (ключи) меняют поведение и начинаются с дефиса: короткие (-l) или длинные (--all). Флаги часто объединяют: -la это -l и -a вместе. Понимание этой структуры превращает «непонятную строчку» в читаемую: видно, какая программа, над чем и с какими настройками работает.',
    example: 'В команде npm run build --silent: npm — программа, run build — что запустить, --silent — флаг «поменьше вывода».',
    related: ['cli-shell', 'npm-scripts'],
  },
  {
    id: 'cli-path',
    categoryId: 'cli',
    term: 'Пути: абсолютный и относительный',
    short: 'Абсолютный путь — полный адрес от корня (D:\\projects\\codecompass); относительный — от текущей папки (./src, ../).',
    detail:
      'Файлы адресуют путями. Абсолютный путь начинается от корня диска и не зависит от того, где ты находишься. Относительный отсчитывается от текущей папки: . это «здесь», .. это «на уровень выше», ./src это «папка src внутри текущей». В импортах кода то же самое: import из \'../../components\' означает «поднимись на две папки вверх, затем в components».',
    example: 'В CodeCompass импорт import { Card } from \'../../components/Card\' — относительный путь; а D:\\projects\\codecompass — абсолютный путь к проекту.',
    related: ['cli-navigation', 'vc-folder'],
  },
  {
    id: 'cli-navigation',
    categoryId: 'cli',
    term: 'Навигация и файлы: cd, ls, pwd, mkdir, rm',
    short: 'Базовые команды: cd — сменить папку, ls — показать содержимое, pwd — где я, mkdir — создать папку, rm — удалить.',
    detail:
      'cd (change directory) переходит в папку, cd .. — на уровень вверх. ls (или dir в Windows) показывает, что в папке. pwd печатает текущий путь. mkdir создаёт папку, touch/New-Item — пустой файл, rm (Remove-Item) удаляет. Осторожно с удалением: в командной строке нет корзины — rm удаляет сразу и навсегда, особенно rm -rf (рекурсивно и без вопросов).',
    example: 'Чтобы запустить CodeCompass, ты по сути делаешь cd в папку проекта и npm run dev; Claude эти переходы выполняет за тебя в терминале.',
    related: ['cli-command', 'cli-path'],
  },
  {
    id: 'cli-pipe',
    categoryId: 'cli',
    term: 'Конвейер (pipe) и перенаправление',
    short: 'Символ | передаёт вывод одной команды на вход другой; > записывает вывод в файл.',
    detail:
      'Философия командной строки: маленькие программы, соединённые в цепочку. Вертикальная черта | («пайп») берёт то, что напечатала одна команда, и отдаёт это следующей: например «показать список | отфильтровать | посчитать». Символ > перенаправляет вывод в файл (перезаписывая), >> дописывает в конец. Это позволяет собирать из простых команд сложную обработку без единой строчки «настоящего» кода.',
    example: 'При проверках CodeCompass встречались конвейеры вида curl ... | grep ... — «скачай страницу и найди в ней нужную строку».',
    related: ['cli-command', 'cli-grep'],
  },
  {
    id: 'cli-grep',
    categoryId: 'cli',
    term: 'Поиск: grep и wildcards',
    short: 'grep ищет строки по шаблону в тексте/файлах; * и ? — подстановочные знаки для имён файлов.',
    detail:
      'grep фильтрует текст, оставляя только строки, подходящие под шаблон, — незаменим, когда вывод большой, а нужна одна строчка. Подстановочные символы (wildcards) в оболочке подставляют имена файлов: *.ts — все файлы с расширением ts, src/** — всё внутри src на любой глубине. Это ускоряет и поиск, и массовые операции над файлами.',
    example: 'При проверке деплоя CodeCompass использовалась строка curl ... | grep index — «в скачанной странице оставь только строку с именем бандла».',
    related: ['cli-pipe', 'debug-console-log'],
  },
  {
    id: 'cli-env',
    categoryId: 'cli',
    term: 'Переменные окружения',
    short: 'Именованные значения, доступные программам из «окружения» системы, — часто хранят настройки и секреты.',
    detail:
      'Переменная окружения — это пара имя=значение, живущая вне кода, в среде, где запускается программа. Так задают то, что меняется между компьютерами и не должно лежать в коде: пути, режим (разработка/прод), секретные ключи. Программа читает их во время работы. В коде на Node это process.env.ИМЯ, во Vite — import.meta.env.ИМЯ. Именно поэтому секреты кладут сюда, а не в исходники.',
    example: 'GEMINI_API_KEY в recall-app — переменная окружения: локально в .env.local, на проде — в настройках Vercel; код читает её, но она не попадает в репозиторий.',
    related: ['web-env', 'vl-env-vars', 'sec-secrets'],
  },
]
