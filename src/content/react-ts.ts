import type { GlossaryEntry } from '../types'

export const reactTs: GlossaryEntry[] = [
  {
    id: 'rt-vite',
    categoryId: 'react-ts',
    term: 'Vite и dev-сервер',
    short: 'Быстрый инструмент сборки и dev-сервер для современных веб-проектов — мгновенный старт и горячая перезагрузка.',
    detail:
      'Vite запускает локальный сервер разработки с HMR (hot module replacement — обновление одного изменённого модуля без перезагрузки всей страницы и потери состояния), а для продакшена собирает оптимизированный бандл. В dev-режиме он использует нативные ES-модули браузера напрямую, поэтому старт почти мгновенный даже в большом проекте. Настраивается через vite.config.ts, куда подключаются плагины.',
    example: 'В recall-app это vite.config.ts с плагинами React, Tailwind и PWA; команды npm run dev / npm run build.',
    related: ['web-build'],
  },
  {
    id: 'rt-vite-plugin',
    categoryId: 'react-ts',
    term: 'Плагин Vite и middleware',
    short: 'Способ добавить своё поведение прямо в dev-сервер — например эмулировать серверный API, не поднимая отдельный бэкенд.',
    detail:
      'Плагин Vite — объект с хуками, которые вызываются на разных этапах работы сборщика. Хук configureServer даёт доступ к dev-серверу, и через server.middlewares.use() можно подключить обработчик HTTP-запросов — middleware, «прослойку», перехватывающую запрос до основной логики. Это позволяет во время локальной разработки имитировать продакшен-эндпоинт.',
    example: 'В recall-app так реализован локальный /api/gemini — самописный dev-middleware вместо установки Vercel CLI на компьютер.',
    related: ['rt-vite'],
  },
  {
    id: 'rt-react',
    categoryId: 'react-ts',
    term: 'React (компоненты, JSX, props)',
    short: 'Библиотека для построения интерфейса из переиспользуемых компонентов-функций.',
    detail:
      'React описывает UI как дерево компонентов, каждый из которых — функция, возвращающая JSX (HTML-подобный синтаксис прямо в коде). Данные передаются вниз по дереву через props (параметры компонента), а локальное состояние компонента хранится в хуках (useState и другие). При изменении состояния React перерисовывает только то, что реально изменилось, а не всю страницу.',
    example: 'Корневой компонент App в recall-app и всё дерево вложенных компонентов features/*.',
    related: ['rt-context', 'rt-useeffect'],
  },
  {
    id: 'rt-context',
    categoryId: 'react-ts',
    term: 'React Context и свой хук',
    short: 'Способ дать многим компонентам доступ к общим данным, не прокидывая props через каждый промежуточный уровень вручную.',
    detail:
      'Context создаётся через createContext, Provider оборачивает часть дерева компонентов и раздаёт значение всем потомкам, а любой потомок читает его через useContext. Хорошая практика — обернуть это в свой хук (например useAuth), который заодно проверяет, что компонент действительно находится внутри Provider, и бросает понятную ошибку, если нет.',
    example: 'AuthContext в recall-app — AuthProvider хранит сессию пользователя, а хук useAuth даёт к ней доступ с любого экрана.',
    related: ['rt-react'],
  },
  {
    id: 'rt-useeffect',
    categoryId: 'react-ts',
    term: 'useEffect и функция очистки (cleanup)',
    short: 'Хук для «побочных эффектов» — того, что не относится к чистой отрисовке: запросов, подписок, таймеров.',
    detail:
      'useEffect(fn, deps) запускает fn после рендера; массив зависимостей deps определяет, когда эффект повторится (пустой массив [] — один раз при монтировании компонента). Если эффект что-то «подписывает» (слушатель события, соединение с сервером), он обязан вернуть функцию очистки, которую React вызовет при размонтировании компонента — иначе возникают утечки памяти и «зомби»-подписки, продолжающие работать после того, как экран уже закрыт.',
    example: 'В recall-app так слушаются изменения авторизации Supabase, а subscription.unsubscribe() в функции очистки отписывается при размонтировании.',
    related: ['rt-react'],
  },
  {
    id: 'rt-router',
    categoryId: 'react-ts',
    term: 'React Router и защищённый маршрут (Protected Route)',
    short: 'Библиотека навигации между «экранами» в одностраничном приложении (SPA) без перезагрузки страницы браузером.',
    detail:
      'В SPA сервер отдаёт один HTML-файл, а какой именно экран показать — решает JavaScript по текущему URL. React Router сопоставляет пути с компонентами через <Routes>/<Route>, а вложенные роуты дают общий каркас (навигацию, шапку), внутри которого меняется только содержимое. Protected Route — компонент-«привратник»: читает состояние авторизации и либо показывает вложенный контент, либо перенаправляет на страницу входа, не дублируя эту проверку в каждом отдельном экране.',
    example: 'Дерево роутов и ProtectedRoute, оборачивающий весь Layout, в App.tsx проекта recall-app.',
    related: ['rt-context'],
  },
  {
    id: 'rt-typescript',
    categoryId: 'react-ts',
    term: 'TypeScript как единый контракт типов',
    short: 'Надстройка над JavaScript со статической типизацией — ошибки вроде «передал строку вместо числа» ловятся до запуска кода.',
    detail:
      'TypeScript проверяет типы на этапе компиляции. Хорошая практика в командном/AI-проекте — вынести все общие типы в один файл (например types/index.ts) и использовать его как единый источник правды: структура таблиц БД, ответы функций, пропсы компонентов описываются как interface/type один раз, а весь остальной код их импортирует, а не дублирует описание своими словами.',
    example: 'tsc -b в скрипте build проекта recall-app проверяет типы всего проекта перед сборкой; общие типы лежат в src/types/index.ts.',
  },
  {
    id: 'rt-promise-all',
    categoryId: 'react-ts',
    term: 'Параллельные запросы (Promise.all)',
    short: 'Запуск нескольких независимых промисов одновременно вместо ожидания их по очереди.',
    detail:
      'Если несколько задач не зависят друг от друга, их не ждут последовательно (await a; await b), а запускают вместе через Promise.all([a, b]) — результат приходит, когда завершатся обе задачи, а суммарное время равно самой долгой задаче, а не сумме времени обеих.',
    example: 'В recall-app так параллельно грузятся «новые» и «просроченные» карточки к повторению — двумя независимыми запросами к Supabase одновременно.',
  },
  {
    id: 'rt-promisify',
    categoryId: 'react-ts',
    term: 'Промисификация событийного API',
    short: 'Оборачивание старого callback/event-based API в промис, чтобы можно было писать async/await вместо вложенных колбэков.',
    detail:
      'Многие старые браузерные API сообщают результат через события, а не через возвращаемое значение. new Promise((resolve, reject) => {...}) позволяет: в удачном событии вызвать resolve(результат), в ошибке — reject(ошибка). Флаг вроде «уже сработало» защищает от повторного срабатывания промиса — например, и через resolve, и позже через отдельное событие onend.',
    example: 'Функция listen() в recall-app оборачивает событийный SpeechRecognition в промис, чтобы вызывающий код писал просто await listen().',
    related: ['rt-promise-all'],
  },
  {
    id: 'rt-cors',
    categoryId: 'react-ts',
    term: 'CORS',
    short: 'Механизм браузера, разрешающий или запрещающий запрос со страницы одного домена к серверу на другом домене.',
    detail:
      'По умолчанию браузер блокирует запросы на «чужой» origin (домен/порт/протокол, отличный от текущей страницы) — это защита от вредоносных сайтов, ворующих данные пользователя с других сайтов. Сервер может явно разрешить конкретные источники, отдав заголовки Access-Control-Allow-*. Для «сложных» запросов браузер сначала сам, без участия кода, шлёт предварительный запрос OPTIONS (preflight), на который сервер обязан правильно ответить.',
    example: 'Serverless-функция api/gemini.ts в recall-app отдельно обрабатывает ветку OPTIONS с нужными заголовками, чтобы её можно было звать и с localhost, и с задеплоенного домена.',
  },
  {
    id: 'rt-webspeech',
    categoryId: 'react-ts',
    term: 'Web Speech API (синтез и распознавание речи в браузере)',
    short: 'Встроенные в браузер бесплатные функции озвучки текста (TTS) и распознавания речи (STT) — без сервера и без ключей.',
    detail:
      'speechSynthesis плюс SpeechSynthesisUtterance озвучивают текст и работают почти во всех браузерах. Распознавание речи (SpeechRecognition) поддерживается заметно уже — практически только в Chrome/Edge, поэтому перед использованием проверяют его наличие (feature detection). Распознавание событийное: результат приходит в колбэк onresult, ошибка — в onerror, а конец сессии — в onend.',
    example: 'lib/speech.ts в recall-app с функциями speak, listen, isRecognitionSupported.',
    related: ['rt-promisify'],
  },
  {
    id: 'rt-llm-params',
    categoryId: 'react-ts',
    term: 'Параметры вызова LLM (system, temperature, токены)',
    short: 'Настройки, которыми задаётся поведение и «стоимость» ответа языковой модели при вызове через API.',
    detail:
      'Диалог с LLM оформляется как массив сообщений с ролями (user/assistant/system) — system задаёт общий «характер» и правила поведения модели. temperature регулирует случайность ответа (ближе к 0 — предсказуемо и строго, ближе к 1 — более креативно и разнообразно), maxOutputTokens ограничивает максимальную длину ответа и, соответственно, стоимость. У некоторых моделей есть отдельный бюджет на «размышления» перед финальным ответом, который можно урезать ради скорости.',
    example: 'Сборка тела запроса к Gemini с system-инструкцией и настройками генерации — в api/_core.ts проекта recall-app.',
  },
  {
    id: 'rt-error-ux',
    categoryId: 'react-ts',
    term: 'Понятные сообщения об ошибках API',
    short: 'Перевод сырых технических кодов ошибок в человекочитаемый текст для конечного пользователя.',
    detail:
      'Сырая ошибка вроде «HTTP 429» ничего не говорит обычному пользователю. Хорошая практика — по коду ответа формировать понятное сообщение: 429 (превышен лимит запросов) → «дневной лимит исчерпан, попробуй позже», пустой ответ → «переформулируй запрос». В JavaScript исключение технически можно бросить чем угодно, не только объектом Error, поэтому текст ошибки безопасно достают конструкцией вида `e instanceof Error ? e.message : \'запасной текст\'`.',
    example: 'Обработка кодов ошибок Gemini API в api/_core.ts и api/gemini.ts проекта recall-app.',
    related: ['rt-llm-params'],
  },
]
