import type { GlossaryEntry } from '../types'

export const webdev: GlossaryEntry[] = [
  {
    id: 'web-client-server',
    categoryId: 'webdev',
    term: 'Клиент и сервер',
    short: 'Клиент — то, что видит пользователь (браузер/телефон); сервер — программа, которая отвечает клиенту откуда-то издалека.',
    detail:
      'Когда открываешь приложение в браузере, браузер — это клиент: он запрашивает данные и показывает интерфейс. Где-то в другом месте (в облаке) работает сервер — программа, которая хранит данные и отвечает на запросы клиента. В Recall «сервером» по сути выступают Supabase (для данных) и Vercel (для AI-запросов через api/gemini) — не нужно арендовать и настраивать физический сервер самому, это называется «как сервис» (as a service).',
    example: 'Когда жмёшь «Показать ответ» в Колоде — это чисто клиентское действие в браузере, а когда карточка сохраняется в базу — это уже запрос клиента к серверу Supabase.',
    related: ['web-api', 'web-database'],
  },
  {
    id: 'web-frontend-backend',
    categoryId: 'webdev',
    term: 'Frontend и backend',
    short: 'Frontend — код, который работает у пользователя в браузере (то, что видно); backend — код, который работает на сервере (то, что не видно).',
    detail:
      'Frontend в Recall — это папка src (React-компоненты, стили, всё, что рисует интерфейс). Backend — это Supabase (база данных + правила доступа) и api/gemini.ts (серверная функция на Vercel). Backend часто прячет секреты (ключи API), потому что код backend не отправляется в браузер пользователя и его нельзя подсмотреть через «Инструменты разработчика».',
    example: 'Именно поэтому ключ GEMINI_API_KEY в Recall хранится БЕЗ префикса VITE_ — так он остаётся только на backend и не попадает в код, который скачивает браузер.',
    related: ['web-client-server', 'web-env'],
  },
  {
    id: 'web-api',
    categoryId: 'webdev',
    term: 'API',
    short: 'Согласованный «интерфейс общения» между двумя программами — набор конкретных запросов, которые можно послать, и ответов, которые придут.',
    detail:
      'API (Application Programming Interface) — это как меню в кафе: не нужно идти на кухню готовить самому, достаточно выбрать пункт из списка и получить результат в понятном формате. Например, Free Dictionary API принимает слово и возвращает JSON с переводом и произношением — не нужно знать, как устроен их словарь внутри.',
    example: 'lib/dictionary.ts в Recall — это код, который обращается к Free Dictionary API по конкретному слову и получает готовый ответ.',
    related: ['web-json', 'web-client-server'],
  },
  {
    id: 'web-json',
    categoryId: 'webdev',
    term: 'JSON',
    short: 'Универсальный текстовый формат для передачи структурированных данных между программами.',
    detail:
      'JSON (JavaScript Object Notation) выглядит как вложенные фигурные скобки с парами «ключ»: значение — { "word": "hello", "translation": "привет" }. Почти все API общаются именно в этом формате, потому что его легко читать и человеку, и почти любому языку программирования.',
    example: 'Ответ от Free Dictionary API, который разбирает lib/dictionary.ts, приходит именно в виде JSON.',
    related: ['web-api'],
  },
  {
    id: 'web-database',
    categoryId: 'webdev',
    term: 'База данных',
    short: 'Организованное хранилище, куда программа сохраняет данные так, чтобы их можно было быстро находить и менять.',
    detail:
      'В отличие от обычного файла, база данных умеет отвечать на сложные вопросы вроде «покажи все карточки этого пользователя, у которых срок повторения прошёл» — быстро, даже если записей миллионы. Recall использует Postgres (через Supabase) — таблицы вроде cards, decks, review_states, где строки — это записи, а колонки — их поля.',
    example: 'Недавний аудит fsrs.ts в Recall как раз про то, чтобы база данных сама фильтровала нужные карточки, а не браузер выкачивал всё и фильтровал сам.',
    related: ['web-api', 'web-supabase'],
  },
  {
    id: 'web-supabase',
    categoryId: 'webdev',
    term: 'Supabase',
    short: 'Сервис, который даёт готовую базу данных (Postgres) + вход пользователей (Auth) + правила доступа — бесплатно на старте, без своего сервера.',
    detail:
      'Раньше, чтобы завести базу данных, нужно было арендовать сервер, установить Postgres, настроить безопасность вручную. Supabase даёт всё это через сайт и код: создаётся проект на supabase.com, выдаётся URL и ключ — и фронтенд уже может напрямую читать/писать данные, соблюдая правила доступа (RLS), которые настраиваются отдельно.',
    example: 'В Recall src/lib/supabase.ts — единственная точка входа для связи с Supabase; URL и ключ лежат в .env.local.',
    related: ['web-database', 'web-rls', 'web-auth'],
  },
  {
    id: 'web-rls',
    categoryId: 'webdev',
    term: 'RLS (Row Level Security)',
    short: 'Правила Postgres/Supabase, которые определяют, какие именно строки таблицы видит и может менять конкретный пользователь.',
    detail:
      'Без RLS любой, кто знает адрес базы, теоретически мог бы прочитать чужие данные. RLS — это правила прямо на уровне базы данных вроде «пользователь может видеть только свои карточки (где owner_id = его id)» — и это работает, даже если кто-то обратится к базе напрямую, минуя фронтенд.',
    example: 'Комментарий в fsrs.ts «RLS показывает только наши review_states» — прямая ссылка на такое правило.',
    related: ['web-supabase', 'web-auth'],
  },
  {
    id: 'web-auth',
    categoryId: 'webdev',
    term: 'Аутентификация (Auth)',
    short: '«Докажи, что ты — это ты» — обычно через email+пароль или соцсеть — чтобы приложение знало, чьи данные показывать.',
    detail:
      'Аутентификация (кто ты) — не то же самое, что авторизация (что тебе разрешено). Supabase Auth берёт на себя всю сложную часть: хеширование паролей, токены сессии, восстановление доступа — не нужно писать это с нуля, что было бы источником серьёзных уязвимостей у новичка.',
    example: 'AuthContext.tsx в Recall — React-обёртка, которая знает, залогинен ли пользователь прямо сейчас, а ProtectedRoute не пускает дальше, если нет.',
    related: ['web-rls'],
  },
  {
    id: 'web-deploy',
    categoryId: 'webdev',
    term: 'Деплой (deploy)',
    short: 'Публикация текущей версии кода так, чтобы она стала доступна в интернете всем (или тебе с телефона).',
    detail:
      'Пока запущена команда npm run dev — это работает только на своём компьютере (localhost). Деплой — процесс «собрать финальную версию и выложить её на сервер, доступный по адресу в интернете». Vercel делает это почти без ручной настройки: подключается GitHub-репозиторий, и при каждом git push сама собирается и публикуется новая версия.',
    example: 'Фаза «деплой на Vercel» в плане Recall — после неё приложение станет доступно по постоянной ссылке, а не только на localhost.',
    related: ['web-hosting', 'web-build'],
  },
  {
    id: 'web-hosting',
    categoryId: 'webdev',
    term: 'Хостинг',
    short: 'Место (обычно облачный сервис), где физически «живёт» опубликованная версия сайта/приложения, доступная по ссылке 24/7.',
    detail:
      'Хостинг — это инфраструктура, которая держит сайт «включённым» и отвечает на запросы пользователей. Vercel — один из бесплатных хостингов, заточенный именно под такие проекты (frontend + serverless-функции), поэтому он и выбран в стеке Recall.',
    example: 'После деплоя на Vercel Recall получит адрес вида recall-app.vercel.app, по которому его можно открыть с любого устройства.',
    related: ['web-deploy'],
  },
  {
    id: 'web-env',
    categoryId: 'webdev',
    term: 'Переменные окружения (.env)',
    short: 'Секретные настройки (ключи, пароли, адреса), которые хранятся отдельно от кода и не попадают в репозиторий.',
    detail:
      'Код в репозитории видят (потенциально) все, у кого есть доступ к GitHub. Ключи API, пароли от базы — если их прямо вписать в код, они «утекут» вместе с ним. Такие значения выносят в отдельный файл (.env.local), который добавлен в .gitignore, а код читает их во время работы через import.meta.env.',
    example: 'VITE_SUPABASE_URL и VITE_SUPABASE_ANON_KEY в .env.local Recall — переменные с префиксом VITE_ так и должны быть, они безопасны на фронтенде; а GEMINI_API_KEY — без префикса, потому что должен остаться только на сервере.',
    related: ['web-frontend-backend'],
  },
  {
    id: 'web-build',
    categoryId: 'webdev',
    term: 'Билд (build)',
    short: 'Процесс превращения исходного кода (TypeScript, JSX, много файлов) в оптимизированные HTML/CSS/JS файлы, которые понимает браузер.',
    detail:
      'Браузер не умеет напрямую исполнять TypeScript и React JSX — это удобный язык для человека, но не для браузера. Команда сборки (в Recall это npm run build → сначала tsc -b проверяет типы, потом vite build упаковывает всё в компактные файлы) превращает код в то, что реально загрузится и заработает на телефоне.',
    example: 'npm run build — «проверка типов + сборка» — запускается после изменений, чтобы убедиться, что ничего не сломано, прежде чем коммитить.',
    related: ['web-deploy'],
  },
]
